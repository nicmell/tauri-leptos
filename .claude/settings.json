{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.rs) rustfmt --edition 2024 \"$f\" && leptosfmt --quiet \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting Rust file"
          }
        ]
      }
    ]
  }
}

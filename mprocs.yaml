# Browser dev loop: `mprocs` starts both watchers in one terminal
# (backend restart-on-change + UI hot reload); browse http://127.0.0.1:1420.
procs:
  server:
    cmd: ["bacon", "serve"]
  ui:
    cmd: ["trunk", "serve"]
